# e-plantShopping

Simple React + Vite shopping cart demo for plants.

Quick start:

```bash
npm install
npm run dev
```

Build & preview:

```bash
npm run build
npm run preview
```

Notes:
- Cart logic implemented in `src/CartSlice.jsx`.
- Update the `base` setting in `vite.config.js` when deploying to GitHub Pages.